# API Autotests Skeleton

Scaffold for API autotests based on the reference architecture:
- `config/` for environment and headers
- `services/` for API clients grouped by domain
- `src/` for shared generators/data/enums
- `utils/` for helper utilities and token/env access
- root `conftest.py` for pytest fixtures and hooks

No test modules are included yet by request.
